# Core dependencies - only what's needed for the Folio app
pandas==2.2.1
numpy==1.26.4
scipy==1.11.3  # For option calculations

# Utilities
requests==2.31.0
PyYAML==6.0.1
typing_extensions>=4.7.1  # For backporting newer typing features to Python 3.9

# Data source
yfinance>=0.2.37  # For portfolio beta calculation

# Web application - using latest versions for security updates
dash>=2.14.2
dash-bootstrap-components>=1.5.0

# ASGI server for production - always use latest for security
uvicorn>=0.27.1
